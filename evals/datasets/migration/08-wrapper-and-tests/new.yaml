openapi: "3.0.3"
info: {title: Example API, version: "2"}
paths:
  /v1/chat/completions:
    post:
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                model: {type: string}
                max_completion_tokens: {type: integer}
      responses:
        '200':
          description: OK
          content:
            application/json:
